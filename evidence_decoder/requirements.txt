# 다층 디코더는 표준 라이브러리만으로 동작한다.
# HTTP 는 urllib, JSON 은 json, 병렬은 concurrent.futures 를 쓴다.
# 실험 환경에서 pip 설치 실패로 파이프라인이 멈추지 않게 하려는 의도적 선택이다.

# 선택 사항
python-dotenv>=1.0.0   # .env 자동 로드. 없으면 examples/run_live.py 가 직접 파싱한다.

# 영상 근거를 다루고 비전 백엔드가 영상 네이티브 입력을 지원하지 않을 때
# ffmpeg 바이너리가 PATH 에 있어야 프레임 샘플링이 동작한다 (pip 패키지 아님).
#   macOS: brew install ffmpeg
